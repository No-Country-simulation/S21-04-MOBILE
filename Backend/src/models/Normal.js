class NormalDTO {
    constructor(id, postId, description, mediaUrl, createdAt) {
        this.id = id;
        this.postId = postId;
        this.description = description;
        this.mediaUrl = mediaUrl;
        this.createdAt = createdAt;
    }
}