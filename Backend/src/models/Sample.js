class SampleDTO {
    constructor(id, postId, description, createdAt) {
        this.id = id;
        this.postId = postId;
        this.description = description;
        this.createdAt = createdAt;
    }
}  