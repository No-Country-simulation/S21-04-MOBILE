class CommentDTO {
    constructor(id, userId, postId, text, createdAt) {
        this.id = id;
        this.userId = userId;
        this.postId = postId;
        this.text = text;
        this.createdAt = createdAt;
    }
}  